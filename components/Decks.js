import React from 'react';
import {Text, View} from 'react-native';

function Decks() {
    return (
        <View style={{flex: 1}}>
            <Text>Decks</Text>
        </View>
    );
}

export default Decks;
